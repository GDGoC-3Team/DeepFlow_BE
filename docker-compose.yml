services:
  mysql:
    image: mysql:8.0
    ports:
      - "3306:3306"
    environment:
      MYSQL_DATABASE: deepflowdb
      MYSQL_ROOT_PASSWORD: deepflowdb
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
